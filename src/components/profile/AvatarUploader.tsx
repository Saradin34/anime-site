import { readFileAsDataURL, squareCropResize, validateImageFile, dataUrlSizeKB } from '@/lib/imageUtils';
import clsx from 'clsx';
export default function AvatarUploader(){ return <div className={clsx('info-card')}>Avatar</div>; }
