import { apiInfo } from '../services/animeApi';
export function About() { return <div className="page about"><div className="page-title"><h1>Источники данных и план парсера</h1><p>Сайт подготовлен под несколько API и backend-proxy для расширения.</p></div><div className="info-grid">{apiInfo.map((x) => <div key={x} className="info-card">{x}</div>)}</div><section className="doc-card"><h2>Как добавлять AnimeVost / Anime-bit / другие</h2><ol><li>Создать Netlify Function или отдельный Node backend, чтобы обходить CORS и не светить логику парсинга в браузере.</li><li>Сделать единый адаптер, который возвращает структуру Anime и Episode.</li><li>Учитывать robots.txt, правила сайтов и авторские права. Лучше использовать официальные/разрешённые API.</li><li>Для HLS подключён hls.js, поэтому m3u8-ссылки сразу проигрываются в плеере.</li></ol><pre>{`interface Provider {\n  search(query: string): Promise<Anime[]>\n  getById(id: string): Promise<Anime>\n}`}</pre></section></div>; }
